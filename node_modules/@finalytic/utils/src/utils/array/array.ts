import { stringToArray } from './string-to-array';

export function array<T>(array: T | T[], separator?: string): T[] {
  if (array === undefined || array === null) return [];
  if (typeof array === 'string') return stringToArray(array, separator) as any;
  if (Array.isArray(array)) return array;

  return [array];
}

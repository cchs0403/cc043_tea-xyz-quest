import parse from 'parse-duration';

export type Duration = number | string;
export const parseDuration = (time: Duration) => {
  return (typeof time === 'string' ? parse(time) : time) || 0;
};
export const ms = (time: Duration) => {
  return (typeof time === 'string' ? parse(time) : time) || 0;
};

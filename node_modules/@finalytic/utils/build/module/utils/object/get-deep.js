export function getDeep(obj, path, defaultValue) {
    const p = typeof path === 'string' ? path.split(/[,[\].]+?/) : path;
    const s = typeof path === 'string' ? path : path.join('.');
    const result = p
        .filter(Boolean)
        .reduce((result, key) => (result == null ? result : result[key]), obj);
    return result === undefined || result === obj
        ? obj[s] === undefined
            ? defaultValue
            : obj[s]
        : result;
}
export function getFlat(obj, path, defaultValue) {
    if (obj?.[path] !== undefined)
        return obj[path];
    return defaultValue || undefined;
}

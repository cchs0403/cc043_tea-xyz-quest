import parse from 'parse-duration';
export const parseDuration = (time) => {
    return (typeof time === 'string' ? parse(time) : time) || 0;
};
export const ms = (time) => {
    return (typeof time === 'string' ? parse(time) : time) || 0;
};

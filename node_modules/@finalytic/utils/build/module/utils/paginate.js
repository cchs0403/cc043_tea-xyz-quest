export async function paginate(max, 
// biome-ignore lint/suspicious/noConfusingVoidType: <explanation>
task) {
    for (let i = 0; i < max; i++) {
        if (i > max) {
            break;
        }
        if ((await task(i)) === false)
            break;
    }
}

import { type Duration } from '../time';
export declare function waitFor(time: Duration, time2?: Duration): Promise<void>;
type Options = {
    retryEvery?: Duration;
    timeoutAfter?: Duration;
    error?: any;
};
export declare function waitUntil<T>(promise: () => PromiseLike<T> | T, condition: (t: T) => any, options?: Options): Promise<T>;
export declare function waitUntil<T>(promise: () => PromiseLike<T> | T, options?: Options): Promise<T>;
export {};

export const uniqueBy = (sources, key) => {
    return [
        ...new Map(sources.map((item) => [
            !key ? item : typeof key === 'function' ? key(item) : item[String(key)],
            item,
        ])).values(),
    ];
};

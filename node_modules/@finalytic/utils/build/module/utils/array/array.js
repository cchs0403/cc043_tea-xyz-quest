import { stringToArray } from './string-to-array';
export function array(array, separator) {
    if (array === undefined || array === null)
        return [];
    if (typeof array === 'string')
        return stringToArray(array, separator);
    if (Array.isArray(array))
        return array;
    return [array];
}

"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sortBy = void 0;
const sortBy = (sources, key, dir = 'asc') => {
    if (!sources)
        return [];
    return sources.sort((a, b) => {
        const valueA = !key
            ? a
            : typeof key === 'function'
                ? key(a)
                : a[String(key)];
        const valueB = !key
            ? b
            : typeof key === 'function'
                ? key(b)
                : b[String(key)];
        if (dir === 'desc')
            return valueA > valueB ? -1 : 1;
        return valueA < valueB ? -1 : 1;
    });
};
exports.sortBy = sortBy;

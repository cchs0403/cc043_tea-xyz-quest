"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.toPgArray = exports.fromPgArray = void 0;
const array_1 = require("./array");
function fromPgArray(str) {
    return str
        .replace(/^{/, '')
        .replace(/}$/, '')
        .split(',')
        .filter((x) => x)
        .map((x) => x.replace(/"/g, ''));
}
exports.fromPgArray = fromPgArray;
function toPgArray(arr) {
    const m = (0, array_1.uniqueBy)(arr, (x) => x)
        .filter((x) => x)
        .map((e) => `"${e}"`)
        .join(',');
    return `{${m}}`;
}
exports.toPgArray = toPgArray;

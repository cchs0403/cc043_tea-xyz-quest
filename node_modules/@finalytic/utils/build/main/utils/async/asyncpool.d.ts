export declare function asyncPool<T, TRes>(iterable: T[], iteratorFn: (item: T, items: T[]) => Promise<TRes>, concurrency: number): AsyncIterable<T>;
export declare namespace asyncPool {
    var all: <T, TRes>(iterable: T[], iteratorFn: (item: T, items: T[]) => Promise<TRes>, concurrency: number) => Promise<T[]>;
}

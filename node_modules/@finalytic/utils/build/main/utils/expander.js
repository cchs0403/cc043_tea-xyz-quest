"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.expander = void 0;
function expander(exp = '') {
    const keys = exp?.split(',').map((x) => x.trim().toLowerCase().split('-').join('')) ||
        [];
    const cache = {};
    return (key) => {
        if (cache[key] === undefined) {
            cache[key] =
                exp === '*' || keys.includes(key.split('-').join('').toLowerCase());
        }
        return cache[key];
    };
}
exports.expander = expander;
